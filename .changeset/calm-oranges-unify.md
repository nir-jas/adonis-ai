---
"adonis-ai": minor
---

Replace the direct OpenAI and Anthropic clients with a shared Vercel AI SDK Core adapter, add a built-in Vercel AI Gateway driver, and expose the adapter for any AI SDK-compatible language model while preserving the existing Adonis AI provider API.
